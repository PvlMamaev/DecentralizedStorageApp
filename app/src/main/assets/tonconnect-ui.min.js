window.TonConnectUI = {
  TonConnectUI: class {
    constructor(options) {
      console.log("✅ TonConnectUI инициализирован с манифестом:", options.manifestUrl);
      this.manifestUrl = options.manifestUrl;
      this.uiOptions = {};
    }

    set uiOptions(opts) {
      this._uiOptions = opts;
    }

    renderWalletConnection() {
      const btn = document.createElement("button");
      btn.textContent = "Подключить Tonkeeper";
      btn.onclick = () => alert("Здесь будет подключение Tonkeeper (эмуляция)");
      const root = document.getElementById(this._uiOptions.buttonRootId);
      if (root) root.appendChild(btn);
    }

    async sendTransaction(tx) {
      console.log("🚀 Отправка транзакции:", tx);
      alert("Транзакция эмулирована (реально не отправлена)");
    }
  }
};